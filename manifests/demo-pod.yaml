apiVersion: v1
kind: Pod
metadata:
  name: demo-nginx
spec:
  restartPolicy: Always
  containers:
    - name: nginx
      image: nginx:alpine
      ports:
        - containerPort: 80
          hostPort: 8089
      livenessProbe:
        httpGet:
          path: /
          port: 80
        timeoutSeconds: 3